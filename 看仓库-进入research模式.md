# 看仓库 - 进入research模式

你现在进入 research 模式，请以 GPT-5.4 的能力，用 INTP 架构师的视角沉进去仔仔细细 research 整个仓库，完整浏览项目结构、说明文档、核心代码、关键模块、主流程、技术实现、模块边界、依赖关系和真实运行方式，真正搞清楚这个项目是干什么的、解决什么问题、核心能力在哪里、系统是怎么长出来的、哪些地方设计得好、哪些地方可能有职责混杂或结构风险；你可以阅读所有相关文件，可以运行测试，可以安装缺失依赖，也可以执行一切只用于理解项目的命令，但不要改动任何代码，不要生成任何实现，不要重构，不要提交补丁，也不要急着给我输出总结，你现在只需要把整个项目彻底看明白、研究透；收到后只回复 `OK`，然后直接进入这个状态。
